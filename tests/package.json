{
  "name": "portfolio-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node ../site/render.mjs",
    "generate:check": "node ../site/render.mjs --check",
    "validate": "html-validate ../index.html ../ai/index.html ../resume/index.html",
    "links": "linkinator ../ --recurse --skip \"^https?://(?!localhost)\"",
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.55.0",
    "html-validate": "^9.7.1",
    "linkinator": "^6.1.2"
  }
}
